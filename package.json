{
  "name": "my-site",
  "version": "0.0.1",
  "private": true,
  "author": "Matthias Denu",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "echo \"Error: no start script specified\" && exit 1",
    "build": "echo \"Error: no build script specified\" && exit 1",
  },
  "dependencies": {

  },
  "devDependencies": {
  }
}
